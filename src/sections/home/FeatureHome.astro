---
import { Icon } from "astro-icon/components";
import { featuredProjects } from "../../data/projects";
---

<!-- Featured Projects Section -->
<section class="py-20 px-4 bg-base-200">
  <div class="container mx-auto max-w-6xl">
    <h2 class="text-4xl font-bold text-center mb-12">Featured Projects</h2>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        featuredProjects.map((proj) => (
          <div class="card bg-base-100 shadow-xl">
            <figure class="px-10 pt-10">
              <div class="relative bg-base-300 w-full aspect-video rounded-xl overflow-hidden">
                {proj.image ? (
                  <>
                    <img
                      src={proj.image}
                      alt={proj.title}
                      class="w-full h-full object-cover"
                    />
                    <div class="absolute inset-0 bg-base-300/20" />
                  </>
                ) : (
                  <div class="flex items-center justify-center h-full">
                    <Icon
                      name="lucide:image"
                      class="h-16 w-16 sm:h-20 sm:w-20 opacity-20"
                    />
                  </div>
                )}
              </div>
            </figure>
            <div class="card-body">
              <h3 class="card-title">{proj.title}</h3>
              <p>{proj.short}</p>
              <div class="card-actions justify-end mt-4">
                  {proj.demo && (
                    <a
                      href={proj.demo}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="btn btn-primary btn-sm sm:btn-md gap-2"
                    >
                      <Icon
                        name="lucide:external-link"
                        class="h-4 w-4 sm:h-5 sm:w-5"
                      />
                      Live Demo
                    </a>
                  )}
              </div>
            </div>
          </div>
        ))
      }

      <!-- Project Card 2 -->
      <!-- <div class="card bg-base-100 shadow-xl">
        <figure class="px-10 pt-10">
          <div
            class="bg-secondary/10 w-full h-48 rounded-xl flex items-center justify-center"
          >
            <Icon name="lucide:sparkles" class="h-20 w-20 text-secondary" />
          </div>
        </figure>
        <div class="card-body">
          <h3 class="card-title">Another Project</h3>
          <p>Another amazing project showcasing your skills and creativity.</p>
          <div class="card-actions justify-end mt-4">
            <a href="#" class="btn btn-primary btn-sm">View Project</a>
          </div>
        </div>
      </div> -->

      <!-- Project Card 3 -->
      <!-- <div class="card bg-base-100 shadow-xl">
        <figure class="px-10 pt-10">
          <div
            class="bg-accent/10 w-full h-48 rounded-xl flex items-center justify-center"
          >
            <Icon name="lucide:zap" class="h-20 w-20 text-accent" />
          </div>
        </figure>
        <div class="card-body">
          <h3 class="card-title">Cool App</h3>
          <p>Description of this cool application you built from scratch.</p>
          <div class="card-actions justify-end mt-4">
            <a href="#" class="btn btn-primary btn-sm">View Project</a>
          </div>
        </div>
      </div> -->
    </div>

    <div class="text-center mt-12">
      <a href="/projects" class="btn btn-outline btn-lg">
        View All Projects
        <Icon name="lucide:arrow-right" class="h-5 w-5 ml-2" />
      </a>
    </div>
  </div>
</section>
